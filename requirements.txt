# Anvil itself only needs these:
pyyaml>=6.0         # read ops/<op>/ and write metadata.yaml
openai>=1.40        # kernel generator via DeepSeek (OpenAI-compatible API) — default
anthropic>=0.40     # optional: --provider claude

# The JUDGE (okbench) and its deps live in the server venv, NOT here:
#   torch (cu128, must include sm_120), triton, and `pip install -e <OpenKernels>`.
# Anvil shells out to that okbench; it does not import torch/triton directly.
